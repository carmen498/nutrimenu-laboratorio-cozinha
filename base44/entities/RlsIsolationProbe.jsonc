{
  "type": "object",
  "properties": {
    "usuario_dono_id": {
      "type": "string"
    },
    "marker": {
      "type": "string"
    },
    "is_test": {
      "type": "boolean",
      "default": true
    }
  },
  "required": [
    "usuario_dono_id",
    "marker"
  ],
  "rls": {
    "create": {
      "data.usuario_dono_id": "{{user.id}}"
    },
    "read": {
      "data.usuario_dono_id": "{{user.id}}"
    },
    "update": {
      "data.usuario_dono_id": "{{user.id}}"
    },
    "delete": {
      "data.usuario_dono_id": "{{user.id}}"
    }
  },
  "name": "RlsIsolationProbe"
}